export default function sitemap() {
  return [
    {
      url: 'https://pinimagegrab.com',
      lastModified: new Date(),
      changeFrequency: 'daily',
      priority: 1,
    },
    // Add more URLs here if the site expands (e.g., /faq, /blog)
  ]
}
